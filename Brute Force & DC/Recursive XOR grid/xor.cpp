#include<bits/stdc++.h>

using namespace std;

void solve(vector<int> &a, int len,int &max,int &ans){
    if(len == 1){
        
    }
}

int main(){
    int n; 
    cin >> n;
    vector<int> a(n);
    for(int i =0;i<n;i++) cin >> a[i];

}